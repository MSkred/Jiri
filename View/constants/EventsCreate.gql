import gql from 'graphql-tag'

export const CREATE_EVENT_MUTATION = gql `
    mutation CreateEvent($academicYear: String!, $courseName: String!, $authorId: ID!, $softDelete: Boolean!, $jurysIds: [ID!]) {
        createEvent(academicYear: $academicYear, courseName: $courseName, authorId: $authorId, softDelete: $softDelete, jurysIds: $jurysIds) {
            academicYear
            courseName
        }
    }
`;